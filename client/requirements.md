## Packages
framer-motion | Smooth animations for page transitions and UI elements
recharts | Data visualization for admin dashboard statistics
date-fns | Formatting dates for company incorporation and AGM details

## Notes
Authentication uses Replit Auth (provided in context) via useAuth hook
File upload endpoint expects multipart/form-data
Admin check uses /api/admin/check
